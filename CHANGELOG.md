# 更新日志

## v2026.09.12 — MCP 批量制作与 Fstudio_skills

本次更新减少了制作 HMI 时逐个查找控件、逐层读取模型和逐项提交属性的调用，并把原型还原、公共页面所有权和布局验收方法整理为可复用技能。

### 新增 MCP 接口

| 接口 | 解决的问题 | 使用范围 |
| --- | --- | --- |
| `fstudio_canvas_snapshot` | 一次获取当前页身份、控件名称、UUID、会话句柄及实际边界，减少逐控件查询 | 支持名称筛选、分页、重名/缺失诊断和活动页 UUID 校验；只包含当前页顶层控件 |
| `fstudio_model_get_many` | 一次读取多个模型的已知深层属性，减少获取中间对象的往返 | 每批最多 100 项、点分隔路径最多 16 段 |
| `fstudio_model_apply_many` | 多个控件统一设置宽度、颜色或对齐坐标等共用属性 | 同页或同草稿作用域，展开后最多 100 个属性修改 |
| `fstudio_canvas_create_many` | 将创建草稿、属性配置和插入合成一次任务 | 每批 1–30 控件，支持最多 20 个共用预设、原生静态文字和本地地址初始化 |

### 原有流程改进

- `model_get` 和 `model_set_many` 支持从根模型按点分隔路径访问属性，例如 `Position.Width`，无需为每一层先获取句柄。
- 批量修改增加路径、类型、作用域、别名重复、父子替换冲突及值类型装箱写入检查；保留原生事务和失败回滚机制。
- 批量绘图使用一个原生撤销组，返回请求边界、实际边界和 `bounds_adjusted`。字体或最小尺寸造成扩宽时可据此修正单位与邻接控件布局。
- 绘图失败时清理本批草稿并核对页面成员；清理未完成会明确报告。创建名称必须唯一，重复请求不会覆盖已有控件。
- 只读任务等待从固定 100 ms 改为 5、10、20、40、50 ms 退避；等待超时保留原 `job_id`，不重新提交写操作。
- 更新 MCP 初始化提示和使用说明，让 AI 优先快照定位、批量处理，并在陌生控件上先验证单个实例。

### 配套 Fstudio_skills

新增可分发的 `skills/fstudio-skills`，包括技能入口、按需参考、AI 调用提示和只读布局检查脚本：

- 根据实际工艺、点表及原型拆解页面，使用原生文字、输入与状态控件。
- 明确背景、顶栏、导航和业务内容的唯一所有者，避免子页重复制作公共部分。
- 按任务选择快照、批量读取、共用属性、差异修改或批量创建；旧版缺少接口时使用兼容流程。
- `check_layout.py` 检查声明的尺寸、对齐、间距和公共组件禁入名单，拒绝分页未取完整、混页或重复 UUID 的输入。
- 按结构、保存重开、编译、模拟和硬件分别验收，并记录可继续使用的稳定名称、预设和版本信息。

### 本地性能样本

| 工作量 | 原调用次数 → 新调用次数 | 原中位耗时 → 新中位耗时 | 样本 |
| --- | --- | --- | --- |
| 25 个控件的 100 项位置属性读取 | 100 → 1 | 101.4 ms → 7.8 ms | 七轮交替测试 |
| 创建 10 个矩形，各配置一个圆角开关属性 | 30 → 1 | 1,180.7 ms → 83.6 ms | 最后一轮三组交替测试 |

测试环境为本机 Windows、FStudio `3.0.15685.0` 和 F010 工程副本，通过真实 stdio MCP 与原生宿主执行。计时不含 AI 思考、网络、编译或硬件运行；轮询不计为业务调用，结果不代表整套 HMI 制作的固定提速倍数。

### 验证与边界

- 26 项 Python 测试及 22 项模型属性行为检查通过，技能格式、引用链接和分发包一致性检查通过。
- 五种混合控件完成创建、配置、保存重开及原生编译：矩形、静态文字、数值显示、数值输入和椭圆。已核对输入模式、本地地址、独立 UUID 和实际边界。
- 快照完成筛选、分页、重名诊断、错误活动页拒绝及重开后身份一致性检查；四种非法绘图批次未留下半成品，正式工程未被改写。
- 快照不合成公共页面，也不展开组合图元内部；布局脚本只检查声明的几何关系。它们不能替代截图、触摸键盘、跨页状态和 PLC 通信验收。
- 本次更新不扩大全部控件模式的支持结论，原生编译器的既有限制仍适用。源码发布排除测试工程、过程脚本、编译产物及厂商资源。

### 如何升级

1. 保存工程，正常退出加载旧插件的 FStudio，并停止客户端中的 MCP 服务。
2. 在仓库目录更新并重新构建：

   ```powershell
   git pull --ff-only
   & 'C:\Program Files\PowerShell\7\pwsh.exe' -NoLogo -NoProfile -File '.\build.ps1'
   & 'C:\Program Files\PowerShell\7\pwsh.exe' -NoLogo -NoProfile -File '.\install-visible-host.ps1'
   ```

3. 自定义 FStudio 安装目录时，沿用安装说明中的 `-FStudioBin` / `-FStudioRoot` 参数。将 `skills/fstudio-skills` 更新到 AI 的技能目录；Codex 的默认目录为 `~/.codex/skills/fstudio-skills`。
4. 重新启动 MCP 服务和加载插件的 FStudio，确认新工具可用。工程重开后重新获取模型句柄。

服务脚本与宿主 DLL 必须一起更新；旧宿主不会热加载新 DLL。遇到 DLL 占用时，安装器会暂存更新，保存并正常重启后再加载。

详细用法见 [高效模型操作](https://github.com/TingRuazhang/FStudio_MCP/blob/v2026.09.12/docs/efficient-model-workflow.md) 与 [Fstudio_skills](https://github.com/TingRuazhang/FStudio_MCP/tree/v2026.09.12/skills/fstudio-skills)。许可证保持标准 MIT；FStudio 软件及相关厂商资源的版权归繁易公司及相应权利人所有。
